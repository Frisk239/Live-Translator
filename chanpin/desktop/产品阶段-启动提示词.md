# 产品阶段启动提示词

复制下面框内全文，交给新会话。

---

你在 `E:\code\Live-Translator`。这是「直播同传工具」的产品阶段，不是再烤需求，也不是写正式客户端。

## 先读（按这个顺序）

1. `CONTEXT.md` — 只用这里的词。禁止：用户、主播、云端模式、本地模式、字幕流
2. `chanpin/desktop/产品阶段-入口.md` — 已定 / 未定，未定不要写成已拍板
3. `docs/v1-形态.md` — 当前唯一要做的形态
4. `docs/adr/0001-audio-source-by-process.md`
5. `docs/adr/0002-cut-follows-speech.md`
6. `docs/adr/0003-streaming-draft.md`
7. `docs/adr/0006-ship-local-first.md`

`docs/v2-形态.md` 和 `docs/后续.md` 只当边界：托管听译、浏览器插件、LLM 都不进这一版。

产品物只写进 `chanpin/desktop/`。程序代码只进 `desktop/`。

## 用哪些 skill

- 界面和状态要看见才知道 → `/prototype`（扔掉的原型）
- 把已定内容收成可开发说明 → `/to-spec`（这里的 spec 就是 PRD）
- 先看 `/ask-matt` 确认走主线。不要 `/grill-with-docs` 从零再烤，不要 `/wayfinder`

## 这一版是什么

Windows 观众端。看别人的英/日/韩直播，本机听译成简体中文。托盘 → 控制面板选音源 → 开听 → 字幕窗。一条字幕条，先草稿后定稿，切条跟嘴，静默约两秒撤条。

## 你必须自己带建议、不能假装已定的

见 `chanpin/desktop/产品阶段-入口.md` 里那 12 条。

## 交付

1. 可交互原型：控制面板 + 字幕窗（假听译即可，能点能看状态），放 `chanpin/desktop/`
2. PRD / spec：用词库的话，写清开听/停止/托盘/失败，标清哪些是这一版新建议，放 `chanpin/desktop/`

不要改 `reference/`。不要实现真的识别翻译。不要建 server/client 目录。
