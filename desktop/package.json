{
  "name": "live-translator-desktop",
  "private": true,
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "python tools/prepare_runtime.py && tauri build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/ws": "^8",
    "typescript": "^5.6",
    "vite": "^8.2.1",
    "vitest": "^2",
    "ws": "^8"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
