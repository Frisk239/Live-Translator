# 托管第一版一个进程两模块，账号和在听会话进 Postgres

第一版托管服务一个 OS 进程：账号模块和听译调度模块，听译引擎可换。壳仍只认一条听译缝。账号和在听会话进 Postgres，这样以后加机器时顶号仍认同一批账号；满员仍是这台机器自己的路数闸。第一天不拆两个 OS 进程，也不做多机分配——单机上路数不因此变多。真要故障隔离或把听译搬到 GPU，再按模块边界拆。

落地补丁见 0032：第一版单机默认内嵌 SQLite 自足；配 Postgres DSN 时即本决定的多机形态，schema 与行为同一份。
